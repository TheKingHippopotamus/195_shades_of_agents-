# .github/workflows/ci.yml
# CI pipeline for pull requests — typecheck, lint, test, build

name: CI

on:
  pull_request:
    branches: [main]

defaults:
  run:
    working-directory: application/production/nexus-platform-e2e/nexus-website

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: application/production/nexus-platform-e2e/nexus-website/package-lock.json
      - run: npm ci
      - run: npx astro check

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: application/production/nexus-platform-e2e/nexus-website/package-lock.json
      - run: npm ci
      - run: npx eslint src --max-warnings 0
      - run: npx prettier --check .

  test-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: application/production/nexus-platform-e2e/nexus-website/package-lock.json
      - run: npm ci
      - run: npx vitest run --coverage --passWithNoTests

  build:
    runs-on: ubuntu-latest
    needs: [typecheck, lint]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: application/production/nexus-platform-e2e/nexus-website/package-lock.json
      - run: npm ci
      - name: Fetch Sanity CMS content
        run: npx tsx scripts/fetch-sanity-content.ts
        continue-on-error: true
        env:
          SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
          SANITY_DATASET: ${{ secrets.SANITY_DATASET }}
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
      - run: npm run build
        env:
          NEXUS_REPO_ROOT: ${{ github.workspace }}
          SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
          SANITY_DATASET: ${{ secrets.SANITY_DATASET }}
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
          # Add RESEND_API_KEY to GitHub repo secrets: Settings > Secrets > Actions
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      - name: Generate search index
        run: npx pagefind --site dist
        continue-on-error: true

      # Lighthouse audit on built site
      - name: Lighthouse audit
        run: |
          npx serve dist -l 4321 &
          sleep 3
          npx lighthouse http://localhost:4321/195_shades_of_agents-/ \
            --output json --output-path ./lighthouse.json \
            --chrome-flags="--headless --no-sandbox"
          node -e "
            const r = require('./lighthouse.json');
            const scores = {
              performance: r.categories.performance.score * 100,
              accessibility: r.categories.accessibility.score * 100,
              'best-practices': r.categories['best-practices'].score * 100,
              seo: r.categories.seo.score * 100,
            };
            console.log('Lighthouse scores:', scores);
            const failed = Object.entries(scores).filter(([k, v]) => v < 90);
            if (failed.length) {
              console.error('FAILED:', failed);
              process.exit(1);
            }
          "
        continue-on-error: true # Non-blocking until site has full content

  test-e2e:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: application/production/nexus-platform-e2e/nexus-website/package-lock.json
      - run: npm ci
      - name: Fetch Sanity CMS content
        run: npx tsx scripts/fetch-sanity-content.ts
        continue-on-error: true
        env:
          SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
          SANITY_DATASET: ${{ secrets.SANITY_DATASET }}
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
      - run: npm run build
        env:
          NEXUS_REPO_ROOT: ${{ github.workspace }}
          SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
          SANITY_DATASET: ${{ secrets.SANITY_DATASET }}
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
          # Add RESEND_API_KEY to GitHub repo secrets: Settings > Secrets > Actions
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      - run: npx pagefind --site dist
        continue-on-error: true
      - run: npx playwright install --with-deps
      - run: npx playwright test
        continue-on-error: true # Non-blocking until E2E tests exist
